name: Publish Container

on:
  push:
    tags:
      - 'v*.*.*'
    branches:
      - master
      - issue*
      - prepare*
  pull_request:
    branches:
      - master
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  publish-container:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for container
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # For version tags: v0.2.1 -> 0.2.1, 0.2
            type=semver,pattern={{version}}
            # For main branch: use 'latest'
            type=raw,value=latest,enable={{is_default_branch}}
            # For every other branch (including manual workflow_dispatch runs): use the branch name.
            # Without this, non-default/non-issue branches produce zero tags and the build never
            # gets pushed to ghcr.io (silently - build succeeds, nothing appears under Packages).
            type=ref,event=branch,enable=${{ github.ref_name != 'master' }}

      # Build and push
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          # Only push if it's NOT a pull_request
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
